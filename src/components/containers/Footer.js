import Footnote from "../ui/footer/Footnote";

export default function Footer() {
    return (
        <footer className="flex items-center justify-center w-full h-24 border-t">
            <Footnote />
        </footer>
    );
}